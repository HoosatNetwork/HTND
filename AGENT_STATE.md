# AGENT_STATE (session htnd-copy-1d)
updated: 2026-09-18T17:40:00+03:00
phase: audit
active_issue: none in progress. HTN-208 committed f1abbcb16, HTN-207 committed 499758822, HTN-196
  (virtual-genesis discriminator only, see ISSUES.md for scope) committed b687caf80. None pushed
  (never push per instructions). Open: HTN-204 needs_human (and the whole needs_human list in the
  "parked needs_human" note below), plus HTN-196's remaining scope (4 other give-up branches in
  missingBlockBodyHashes, and a duplicate-pruning-point-in-tips observation - both explicitly left
  alone, see ISSUES.md HTN-196).
next_action: mempool audit is now COMPLETE (two fork passes, all production .go files in
  domain/miningmanager/mempool + mempool/model covered). One real bug found and fixed (HTN-209,
  59091739a: GetByIndex bounds check). Second pass (check_transaction_standard.go,
  handle_new_block_transactions.go, remaining model/*.go) found nothing that survived scrutiny -
  checked safe: IsTransactionOutputDust's output.Value*1000 multiply can wrap on a huge peer-supplied
  value but Go wraps rather than panics and the tx still needs real backing inputs to go anywhere, so
  worst case is a wrong dust classification, not a crash or fund bug; checkTransactionStandardInContext's
  input.UTXOEntry deref is pre-guarded by fillInputsAndGetMissingParents (traced); handle_new_block_
  transactions.go's blockTransactions slice always has a coinbase (only ever called with a consensus-
  accepted block, traced from flowcontext/blocks.go:41); HandleNewBlockTransactions confirmed under
  mp.mtx; map_types/mempool_transaction/orphan_transaction/transaction.go are trivial, nothing to find.
  Started app/protocol/flows/v8 pass (self, not a fork - already had deep context on pruning-point/IBD
  mechanics from HTN-196/208): handle_pruning_point_and_its_anticone_requests.go read end to end,
  checked safe (the pruningPointAnticoneSlot release-during-wait/reacquire design is correct - every
  "holds the slot" phase is bounded, the only unbounded wait releases first, and the defer covers
  every return path including disconnect; the `index < 0` check at line 106 and the int->string->uint64
  conversions are dead-code/style oddities, not bugs). ibd_with_headers_proof.go's
  requireStagingConsensus (line 81-89) discards InitStagingConsensusWithoutGenesis's error and can
  return (nil, nil) - traced whether that's reachable: BOTH of its 2 call sites are only ever reached
  through downloadHeadersAndPruningUTXOSet, which is only called from ibdWithHeadersProof AFTER that
  function's own lines 20-38 already guarantee a staging consensus exists (creates it or returns an
  error). So the nil-fallback branch in requireStagingConsensus is unreachable via the only call graph
  today - tried fixing it, then reverted per CLAUDE.md's "don't add handling for scenarios that can't
  happen": I could not produce a live failure scenario, so it doesn't meet this session's bar (prove
  the mechanism, don't guess). Left as-is; worth revisiting only if a new caller of
  requireStagingConsensus is ever added without that guarantee.
  Read handle_relay_invs.go end to end next: found and fixed HTN-210 (constants.SetBlockVersion was a
  check-then-act race, not a CAS - committed b190d32aa; see ISSUES.md for the "not reproduced under
  stress but definitely wrong code" honesty note). Rest of handle_relay_invs.go checked safe on this
  pass (offenseTracker locking, invsQueue/invChan/blockChan/locatorChan channel handling, requestBlock
  hash-match ban, orphan/IBD routing) - nothing else new found.
  2026-09-18 ~19:15: user asked "why do reorgs happen so often, and why do node disagreements happen
  on transactions" - answered by synthesizing the existing HTN-001..012 split-brain audit cluster
  (HTN-002/005 baseline-never-matches-commitment is the confirmed epicenter, HTN-004 is the
  transaction-level mechanism, HTN-001/003/009/010's now-fixed-but-historically-baked-in version
  disparity likely explains HTN-006's measured 62.5% blue-score mismatch rate), then traced PAST
  HTN-198's own fix and found HTN-211: ResolveVirtual's DAGKnight-vs-blue-work "overcome" check
  (added by HTN-198/f1a75eb75) only ran inside the chunking branch: chains short enough to resolve in
  one pass - the common case, since every ordinary IBD catch-up round ends by calling ResolveVirtual -
  skipped the check entirely, so virtual's selected parent swapped to whatever DAGKnight ranked first
  unconditionally, no blue-work check at all. User said "Fix it" (applying the SAME already-decided
  HTN-198 rule to the gap, not a fresh policy question) - FIXED, committed 22529337f. Full gate green
  (domain/consensus/... 53 ok, -tags=ci ./... 110 ok, rest of tree 49 ok, cmd/htnwallet ok,
  testing/integration full non-ci green). New test verified to fail pre-fix (selected parent visibly
  moved to the lighter tip) and pass after.
  Rest of app/protocol/flows/v8 (block_locator.go, handle_ibd_*.go except what HTN-196 touched,
  handle_request_*.go, transactionrelay/*, addressexchange/*, ping/*, rejects/*) NOT yet re-read this
  pass - most already have "checked safe" entries from earlier in this session's history (see the long
  block of "checked safe:"/"audited safe:" notes above), but none of those predate HTN-205/206/207/208.
  HTN-197 and HTN-203 stay needs_human.
  worth checking: whether any OTHER process-global mutable var in the codebase shares
  SetBlockVersion's old check-then-act shape (HTN-210's fix note says this pass was not an exhaustive
  sweep for the pattern).
  next candidate worth investigating (not started): whether HTN-211's fix, deployed to a real node,
  measurably reduces reorg/restamp frequency - would need a live node comparison, can't do from here.
  ISSUES.md status-field distrust note still stands - HTN-194 and HTN-208 both had stale status lines
  found and corrected this session; worth a dedicated pass at some point re-reading every "status:"
  line against what actually shipped.
working_tree: clean except AGENT_STATE.md/ISSUES.md (never staged, per instructions).
last_command: full gate for HTN-196 (see last_result)
last_result: HTN-196 FULL GATE GREEN 2026-09-18 ~17:35 - domain/consensus/... + app/... full suites;
  go test -tags=ci ./... 108 ok; rest of tree 48 ok; cmd/htnwallet -p 1 ok; testing/integration full
  non-ci rerun (all long tests) 50s PASS. staticcheck + gofmt clean repo-wide. New regression test
  (missing_block_body_hashes_test.go) verified to fail against the pre-fix antipast.go (git stash of
  just that file) and pass after.
blocked_reason: none
safety_flags: consensus_touch=yes for HTN-208 (blockInheritsKnownUTXOCommitmentOffset gained a 3rd
  signal; UTXOSetHealth/IsUtxoSetVerified semantics widened - both scoped to the
  imported-pruning-point-offset regime, see f1abbcb16). HTN-207 (499758822) is NOT consensus-visible -
  it only changes which lookups populate an in-memory read cache, not any validated value or DB
  content. HTN-196 (b687caf80) protocol_touch=yes but NOT a validation-rule change: it changes IBD
  control flow only (report failure instead of false success on one specific unreconcilable-chain
  shape, so the node disconnects and retries a different peer) - does not change what any block or
  peer message is accepted/rejected on consensus grounds. genesis_touch=no throughout.
notes:
- HTN-204 IS NOT SAFE TO RE-APPLY as ISSUES.md described it. Re-applying it breaks testing/integration TestIBDWithPruning 3/3 (2/2 pass without). calculateBlockWindowHeap feeds BOTH difficulty AND BlockDAAWindowHashes, which handle_pruning_point_and_its_anticone_requests uses to decide what trusted data to serve; widening the window makes the serving node enumerate blocks TrustedDataDataDAAHeader cannot answer for, and the syncing peer's IBD times out. That is almost certainly why the fix is not in master. Reopened as needs_human.
- integration gate memory: testing/integration Test16IncomingConnections starts 17 in-process nodes. Each gets pebble.Options' 4 GiB default cache (HTND_PEBBLE_CACHE_MB only helps above 256) plus preallocated LRUs, so it needs the machine mostly idle. The other 11 integration tests run fine under a 7G cap with HTND_PEBBLE_CACHE_MB=300 HTND_MEMTABLE_SIZE_MB=32.
- HTN-206 aftermath 2026-09-18 13:2x: with the reindex root fixed, a fresh CPU profile of htnd5 has NO reachability entry in the top 14 at all. The new top cost is HandleGetBalancesByAddresses -> getBalanceByAddress -> FilterUTXOPairsAgainstVirtual -> GetVirtualUTXOEntries at 52% (the HTN-199 address-index workload). Difficulty went from 1.83 this morning to 10,165,227, and the submit rate fell from 32.7 BPS to ~4.5 BPS as difficulty corrected - that is the difficulty catching up, not the collapse returning.
- HTN-206: the mid-session refutation of HTN-205 was WRONG and is retracted. The discriminator is not tree size, it is whether the chain has spent the interval space it inherited. htnd4 looked like a counterexample only because its fast-period LOG was compared against its end-of-life DATADIR. Confirmed live 2026-09-18 13:11: fixing the stuck root took htnd5 from 0.8 to 26.8 BPS.
- SCRATCHPAD IS TMPFS: multi-GB datadir copies under the scratchpad consume RAM, not disk. Three copies took MemAvailable from 18G to 6.5G. Delete them as soon as a measurement is done.
- another agent's patches were reviewed 2026-09-18: the isTrustedData=false->true fallback in findNextReindexRoot does NOT apply here (tested: the blocking child c3003a48 is absent under BOTH flags), and the BeginUnindexed premise "no staging shard calls Get on dbTx" is false - headersselectedchainstore reads highestChainBlockIndex(dbTx), safely, because that read precedes its writes. pebble.Batch.Get PANICS on a non-indexed batch, so the guard is mandatory.
- gate gotcha: `systemd-run --user --scope ... go test ./...` backgrounded via the Bash tool gets SIGTERM (exit 143) after ~1h when the wrapper shell exits, always after 99 packages. The tail (testing/integration, util/*) must be run as its own foreground command, or build with `go test -c` and run the binary.
- /mnt/data/.htnd4/hoosat-mainnet/datadir2 is the intact, idle datadir of the node that kept up with 5 BPS on 2.16.0-0f308b388 (2026-09-04/05). It is the control for any A/B on block processing speed. Copy it, never open it in place.
- HTN-205 method that worked: hardlink-snapshot a live pebble datadir (cp -al into a dir on the SAME filesystem, then cp -a that to the scratchpad, then rm the link dir). A plain cp of a live pebble dir is torn - compaction deletes SSTs mid-copy and the manifest then references a missing file. NEVER open the hardlinked dir itself, its inodes are the live node's.
- HTN-205 method: an offline inspector that builds the real stores over the snapshot (prefixmanager.ActivePrefix -> consensusdatabase.MakeBucket(prefix.Serialize()), reachabilitydatastore.New(prefixBucket,...) for the shared store, ghostdagdatastore.New(prefixBucket.Bucket([]byte{0}),...) for level 0) lets you replay UpdateReindexRoot on a throwaway staging area and see what it would stage without committing anything. Kept in scratchpad/rinspect.
- HTN-205 note: factory.go:236-241 gives EVERY block level the same reachability data store when the old per-level store is uninitialised, so proof-only blocks (no level-0 GHOSTDAG data) end up in the level-0 reachability tree. Not fixed, see HTN-205 "left alone".
- htnd5 2026-09-18: /mnt/data/.htnd5/utxo-survey.jsonl is 7.8 GB (HTND_UTXO_SURVEY / --enable-utxo-debug-diagnostics). Not growing at the moment, but worth telling the user.
- htnctl refuses to talk to a node built from a different commit ("Server version mismatch"); use a tiny program over infrastructure/network/rpcclient instead (scratchpad/probe), which only warns.
- checked: grpc_server Start/Stop/inbound handler, compression fallback handling - no bug
- checked: pebble.ErrNotFound is returned unmarked so std errors.Is works (pebble_test.go:79); only corruption needed cockroachdb Is
- checked: htnctl config rejects empty command; tools/pebble-tool is binary only (no source)
- zsh: "$VAR:tree" applies the :t (basename) history modifier - it expanded /home/.../HTND-copy:tree to "HTND-copyree" and silently skipped runs; always write ${VAR}:suffix and split specs with parameter expansion, not IFS=: read
- zsh: unquoted $VAR is not word-split; use ${=VAR} or arrays for file lists; a bare word starting with = (e.g. echo =====) is =cmd path expansion and aborts the command - quote it
- maintainer decisions 2026-09-14: HTN-128 disconnect on not-found; HTN-116 keep deletion (pebble has no recovery API); HTN-108 keep large anticone limit; HTN-121 fix; add HTN-133 atomic Fee
- someone else committed ace480940 (version bump 2.17.2) and has uncommitted blockheader work in the tree (2026-09-14)
- checked: util.DecodeAddress/bech32 fuzzed 7.7M execs (60s) without panic; charsetInverse is [256]
- checked: all params slices indexed by global block version; every setter derives it from BlockVersionForDAAScore (max len(POWScores)+1) and every network slice is long enough
- checked: trusted-data protowire converters reject nil nested header/ghostdag/hash fields; no other peer indices used as slice indices
- checked: protowire header converter + blockheader.DirectParents guard empty parents
- checked: peer-list indexing in ibd.go (locator hashes, headers) all length-guarded; import/apply pruning points run only after validation
- CI gate check: repo-wide gofmt clean and build_and_test.sh staticcheck subset clean on all touched packages (2026-09-14)
- checked: rpc manager consensusEventsHandler panics on any notification error (kaspad design); triggers need pruning during large event lag or utxoindex errors - not filed
- checked: addressmanager bans keyed by IP in memory and DB, restored at startup - no bug
- checked: RPC ban/unban/add_peer/get_connected_peer_info/get_headers/get_subnetwork/resolve_finality_conflict/get_block_template/get_info/get_block_dag_info - no crash paths
- checked: pruning point UTXO set server (matches kaspad), component manager/app shutdown order (db closed after Stop)
- checked: transaction_acceptance.go, get_usable_addresses.go, get_block_by_transaction_id.go - no bug (errors degrade to no-verdict); skipped utxo-by-address handlers (user active there 2026-09-13)
- checked: miningmanager template cache is disabled (no coinbase mismatch); htnminer templatemanager locks correctly
- checked: htnminer polls templates every 100ms, so dead notification subscription after reconnect is harmless
- rpcclient: reconnect leaks old grpc.ClientConn (never Close), timeout/GRPCClient fields raced; notification loops break (no panic) on route close
- gofmt guard: use test -z "$(gofmt -l ...)" - plain gofmt -l exits 0 even when files are unformatted
- test guard: never judge go test via `cmd | tail` in an && chain - tail exits 0; redirect to a log and check $? (HTN-160 was first committed with its own test failing)
- checked safe: pebble cursor.Value() consumers all UnmarshalVT (vtproto copies bytes) or Put into batch; ldb wrapper has no cursor tracking
- checked safe: RPC handler inputs (hashrate min window 1000, GetBlock(s) header-only), difficultymanager double defer free (free zeroes window, Free(nil) no-op)
- checked: flowcontext/blocks.go, addressexchange, ping - only benign race on FlowContext.lastRebroadcastTime (kaspad same)
- checked safe: handshake (panics only on local hidden --protocol-version misconfig)
- checked safe: protowire nil checks for P2P/RPC tx converters; P2P list messages capped except RequestIBDBlocks (HTN-115)
- checked safe: connmanager (RemoveConnection panic has no callers), addressmanager ban expiry, router.ErrTimeout is non-ban protocol error
- checked safe: domain/consensus/utils/utxo utxoEntry.Equal does an unchecked other.(*utxoEntry) assertion (predates 230e98b86), but *utxoEntry is the only non-test UTXOEntry implementation (the other is a mempool test double)
- checked safe: mempool wallet freezing - isWalletFrozen runs in validateTransactionInContext after fillInputsAndGetMissingParents, and unorphanTransaction runs ValidateTransactionAndPopulateWithConsensusData then validateTransactionInContext, so orphan promotion cannot bypass it; checks input, P2SH redeem-script and output addresses under the manager mutex; inputs without an extractable address are skipped but a frozen address is always a standard, extractable one; the runtime FreezeWallet/UnfreezeWallet/IsFrozen/GetFrozenWallets API has no callers outside the mempool (no RPC), so only config FrozenAddresses apply - raw config strings vs EncodeAddress canonical form is HTN-159's validation scope
- checked safe: mempool compound_tx_rate_limiter - addressTracker map under globalMutex, submissions under each tracker mutex; getOrCreateTracker refreshes lastSeen under globalMutex so cleanupEmptyTrackers cannot drop a tracker just handed out; trackers are created on check but come from real UTXO scripts and are pruned when empty for 2 windows; cleanupOldSubmissions panics only for a window > MaxInt64 minutes (operator config)
- observation, not filed (operator semantics): --max-compound-tx-per-minute is compared against all submissions in --compound-tx-ratelimit-window (default 1), so a window > 1 turns the per-minute limit into a per-window limit; the limiter is always enabled by design (component_manager.go:131, no disable flag); config.go comment says "10+ inputs" for a threshold of 21
- checked safe: consensusstatemanager blockSurvey (HTND_UTXO_SURVEY) used from validateBlockTransactionsAgainstPastUTXO per-transaction goroutines - noteFailure and noteMissingOutpointsFromError lock bs.mu around steps/missing/missingOrder; active() only nil-checks the receiver; failed/errorDetails/errorLabel run in recordBlockSurvey after the goroutines join
- checked safe: defer-with-pre-evaluated-argument sweep (HTN-189 closeRPCClients class) over app, domain, infrastructure, cmd non-test code found only get_balances_by_addresses:82, get_blocks:153 (defer takes the pointer, writes go through it) and get_usable_addresses:125 - all benign (responses use copies; release clears to cap)
- checked safe: every infrastructure/network/rpcclient request method (41 files) reads its reply with DequeueWithTimeout; plain Dequeue only appears in the spawned notification loops, which block by design
- checked safe: rpchandlers get_balances_by_addresses and get_usable_addresses sync.Pools - responses (and the balances cache) use fresh copies (responseEntries make+copy; responseAddresses append to a new slice); the deferred release clears only the pooled array slots; a grown append leaves the new array to GC and returns the original to the pool. Shared-memory/pool sweep of app, domain, infrastructure done with no new bug beyond HTN-193
- checked safe: rpchandlers get_blocks - pooled rpcBlocks slice is cloned into response.Blocks before the deferred release clears it (clearing zeroes pointer slots, not the RPCBlocks); cache hits return the cached slice and RPCBlocks to several clients, but nothing writes them after the handler: protowire RpcBlock/Header/VerboseData fromAppMessage only read the message (the two message.AcceptedTransactionIDs[i] writes in protowire are in toAppMessage on freshly built messages)
- checked safe: blockparentbuilder sync.Pools - returned parents are built from fresh make() slices of candidate.hash pointers to header-owned hashes; pooled candidate maps/reference slices are cleared and Put only in defers after parents is built; candidate maps cleared on Get; toRemove cleared before Put and not returned to the pool on error paths
- checked safe: externalapi DomainHash.String and ScriptPublicKey.String sync.Pool buffers - both copy the hex/bytes into a new string before Put and never touch the buffer after
- checked safe: package-level mutable maps - blockrelay offenseTracker (offenseTrackerLock around read/modify/write and the connection-end delete); rpchandlers balancesByAddressesCache, getBlocksCache, balanceByAddressCache (each has its own mutex around lookup, purge and insert); txscript OpcodeByName filled in init only
- checked safe: 12026a538 block template builder GC diff is behaviour-preserving (selectedTxs nil vs empty only affects the coinbase prepend, which appends either; candidateTxs preallocated and index-filled for every i, the non-native branch still panics; txselection copies the same three slices)
- checked safe: 12026a538 GetTransactionsByAddressesNoClone returns mempool-owned transactions read by get_mempool_entries_by_addresses after mp.mtx is released, but the only unsynchronised in-place mempool writes are input.UTXOEntry (fill_inputs:53, orphan_pool:163/338, revalidate:146) and no reader on that path touches UTXOEntry (DomainTransactionToRPCTransaction reads outpoint/sigscript/sequence/sigops; verbose data uses outputs, IDs and mass); Fee and Mass are atomic (HTN-133); separate struct fields are separate memory for the race model
- observation, not filed: htnwallet daemon splitAndInputPerSplitCounts divides inputCount by inputsPerSplitCount = floor(massForInputs/massPerInput); it would divide by zero only if one input weighs more than ~99.7k mass (split txs carry no payload, so only the change output uses the 100k standard budget) - e.g. ~100 sigops in one multisig input, not realistic. The daemon grpc.NewServer has no panic recovery interceptor (as kaspad), so any handler panic ends the daemon
- checked safe: cmd/htnwallet encodeTransactionsToHex panics on an empty list (make([]byte, -1)), but its callers never pass one: sign fails deserializing an empty input first, and create-unsigned-transaction gets >=1 transaction (selectUTXOs errors on none; maybeSplitAndMergeTransaction returns [tx] or splitCount>=1 splits)
- observation, not filed: 230e98b86 made utxoEntry.ScriptPublicKey() return the internal pointer instead of a clone ("MUST NOT be mutated"); greps for writes to .Script/.Version through the accessor or a variable taken from it found none (2026-09-15); NewUTXOEntry already aliased the caller's Script slice before that
- checked safe: SubmitBlock with a missing header - protowire RpcBlock.toAppMessage passes x.Header to RpcBlockHeader.toAppMessage, which returns errorNil for a nil receiver, so conversion fails before the handler reads req.Block.Header; repeated parents elements are always allocated by protobuf decoding
- checked safe: app/rpc/rpc.go:78 panic on AddIncomingRoute error - only a duplicate command on a fresh router, and the commands come from the static handlers map, so no remote input reaches it
- checked safe: app/rpc/rpccontext/verbosedata.go:148 panics if a block header timestamp is negative (FormatInt then ParseUint), but only accepted, stored blocks reach PopulateTransactionWithVerboseData with a header and consensus requires timestamps above the window past median time (> 0 after genesis) - unreachable
- checked safe: mempool by-address indexes key by raw ScriptPublicKey.String() on insert (transactions_pool.go:290/293, orphan_pool.go:399/402) and get_mempool_entries_by_addresses looks up scriptPublicKey.String() from PayToAddrScript - same encoding, not the HTN-190 bug
- checked safe: rpcclient rpc_on_*.go notification loops break on routerpkg.ErrRouteClosed before panic(err) (plain Dequeue has no other error), so Close() with notifications registered does not exit; grpcserver connection_loops.go:57 panic fires only if the dev-only HOOSAT_EXPERIMENTAL_DELAY env var is not an integer (once, under sync.Once, which also makes the shared blockDelay read race-free); grpcclient handleError/rpcclient disconnect panics need CloseSend to fail, which grpc v1.83.1 never does
- observation, not filed: htnwallet daemon sync() runs every 2s and collectFarAddresses advances nextSyncStartIndex by 100 each time with no stop condition (same design as kaspad kaspawallet); it would reach the 2^31 non-hardened derivation limit only after ~1.36 years of uptime without restart (field resets on restart) - latent, wallet path, left alone
- checked safe: htnwallet bip32 parsePath/parseIndex (index >= 2^31 rejected before the hardened offset, negatives rejected in both branches, uint32 overflow guarded; "m/" fails Atoi, "m" is the root path)
- checked safe: util/difficulty scratch big.Int pool (every get/put balanced, no pooled value returned or used after put); getHashrate divides by target*ms/1000 and panics on a zero divisor, but its only production caller is a Debug log closure in blockprocessor validate_and_insert_block.go:205 on an already-accepted block whose PoW bounds the target > 0 - not filed (that log line also indexes targetTimePerBlock by the global version; log-only)
- checked safe: util/amount.go NewAmount/round convert a negative float to uint64 Amount (implementation-defined), but the only production caller (config.go:575 --minrelaytxfee) rejects <= 0 via minRelayTxFeeIsValid on the original float, and MulF64 has only a test caller - not filed
- checked safe: submit_block.go:155 window-size index (max version len(POWScores)+1 = slice len on all nets); submit tx handlers fine
- SHARED TREE: never rewrite HTN-001..012 blocks wholesale (targeted status/progress edits only); HTN-101+ are this session's IDs
- my issues use HTN-101+; edit ISSUES.md only with targeted in-place edits, never full rewrites
- AGENT_STATE.md and ISSUES.md are NOW TRACKED and no longer gitignored (changed by the user; confirmed 2026-09-18 with git check-ignore returning nothing and git ls-files listing all three of AGENT_PROMPT.md, AGENT_STATE.md, ISSUES.md). The user asks for their state to be committed. Commit them with the docs(agent) subject, separately from code commits.
- fixed: HTN-101 06eb14977, HTN-102 47fc4a2d1, HTN-103 959642cad, HTN-104 975568650, HTN-105 9714fcfce, HTN-109 03f598db7, HTN-110 6a1615559, HTN-112 8b7e1f9e9, HTN-113 fa59e3af0, HTN-118 f1e64a9e9, HTN-119 04d5c9774, HTN-120 7be59287d, HTN-124 61962e159, HTN-117 85d5322ef, HTN-125 e6343fd1c, HTN-126 d7ea98c5b, HTN-127 ee753d8dd, HTN-111 486460b82, HTN-107 3a7b431bc, HTN-131 abdbf55d8, HTN-132 56d7b74f0, HTN-128 494c0280c, HTN-121 7e620e5c0, HTN-133 4a1f26dce, HTN-134 882fe4914, HTN-135 882fe4914, HTN-136 de342eb4b, HTN-137 075dcff20, HTN-138 235ad08d1, HTN-122 235ad08d1, HTN-123 dcd59f8fa, HTN-129 73d1923ca, HTN-139 7dca07ac8, HTN-141 f2ec6bec1, HTN-140 925b3b4d1, HTN-142 7cf0678a6, HTN-143 c692c8a75, HTN-144 4a5923c92, HTN-145 c7bb663dd, HTN-147 b53ae5e0b, HTN-148 1d6defaf5, HTN-149 01dc2a4e1, HTN-150 bd297a91d, HTN-151 90f9891b7, HTN-152 683eeac4b, HTN-154 6fc22a32c, HTN-155 24392910b, HTN-156 09869e60c, HTN-157 e1c494718, HTN-158 f931ee103, HTN-160 d266a16dc, HTN-161 4d512bd14, HTN-163 5d4463e66, HTN-165 e0a2e143b, HTN-167 433c430f2, HTN-169 0aadca68f
- checked safe: htnwallet broadcast release-on-reject - refreshUTXOs runs at the start of every create (not on the 2s ticker; sync() refresh is commented out) and excludes mempool spends (complete since HTN-148)
- checked safe: mempool RBF (conflicts+redeemers, evicted-output guard, fee/rate policy); zero-fee ghost in ordered set unreachable (minrelaytxfee>0 enforced, RelayNonStdTxs false on all nets)
- checked safe: rpc NotificationManager locking/listener lifecycle (RemoveListener deferred), GetBlockDAGInfo cache clones, chain_changed chunking, PruningPointUTXOSetOverride via events chan (virtual sends non-blocking when full, 100k buffer, IBD holds no consensus lock)
- checked safe: v8 transactionrelay (route carries only inv/tx/notfound so plain-error default unreachable; RejectInvalid only on RPC RBF path so relay never bans honest peers; held-tx buffer bounded by count and bytes)
- checked safe: libhtnwallet createUnsignedTransaction, utils.KasToSompi (regex caps 12 int + 8 dec digits; ParseUint rejects overflow)
- checked safe: domain/exodus + htnexodus import (verify before touch, --force, header commitment check, resume trusts only digest-verified prefix); minor: Reader.Iterate allocates the record length a chunk claims (<=4GiB) before ReadFull - lazy pages on Linux, not fixed
- historical (fixed HTN-185 6b8bccd52): minor, not fixed: get_transaction_status.go:99 confirmations = spBlueScore - blockBlueScore + 1 wraps on pending/inconclusive answers if the unmerged containing block has a higher blue score than the selected parent (needs lower blue work with higher blue score - rare; output-only)
- historical (fixed HTN-186 25c113eca): minor, not fixed: addressrandomize weightedRand can return an already-selected (zero-weight) index on float rounding or randPoint==0, yielding a duplicate address in RandomAddresses
- checked safe: addressmanager network.go IsRoutable/GroupKey (net.IP tolerates odd lengths; slices only after 16-byte prefix match), dnsseed seed.go, peer.UpdateFieldsFromMsgVersion; odd-length peer IPs only create junk entries that fail dialing and age out
- method: grep panic( in peer/RPC-reachable packages and triage each
- checked safe: blocktemplatebuilder non-native subnetwork panic unreachable (EnableNonNativeSubnetworks false on all 6 nets; isolation check runs for every mempool tx incl. orphans)
- policy note, not changed: relayed compound txs in the transaction pool never expire/evict (78fcbddaf); valid txs leave when mined or double-spent - acknowledged tension in that commit; HTN-145 fixed only the orphan-pool side
- checked safe: unchecked message assertions in flows all on single-command routes except HandleRequestHeaders (HTN-147); two-command serving flows (pruning point UTXO set, pruning point and anticone) use checked assertions
- checked safe: handle_ibd_request_chain_block_locator nil high/low hash (converter allows nil; handler builds full locator), request converters reject nil hashes via Hash.toDomain
- checked safe: handle_relay_block_requests and handle_ibd_block_requests (missing block -> protocol error; serveRequestedBlocks waits for all workers), handle_request_block_locator (limit 0 -> empty -> protocol error; locator log-sized). blockrelay serving side done (anticone requests covered by HTN-108 wontfix)
- user active on utxo-by-address rpchandlers (6073ca360 2026-09-12, 77b0e61fb 2026-09-13): audit only for crash-level issues
- checked safe: get_mempool_entries.go (pool/orphan slices kept separate), get_balances_by_addresses.go (pooled slice copied before caching; cache under mutex)
- checked safe (HTN-123 regression): rpcclient rpc_on_* notification loops exit via ErrRouteClosed when Reconnect closes the old router (Dequeue returns only ErrRouteClosed); notifications stop after reconnect as before (server listeners are per connection)
- checked safe: flow indexing of peer slices (loops len-guarded; pruning proof validation rejects empty proof/level 0 before ibd_with_headers_proof.go:200; empty pruning points = HTN-130); no division/modulo by len in app, network, miningmanager, utxoindex, htnwallet
- checked safe: package-level mutable maps all mutex-guarded (offenseTracker, getBlocks/balance/usableAddresses caches, util/memory allocations, RPCStats); htnwallet flockMap unguarded but only used by keys file lock (no concurrent callers found)
- policy note, not changed: handle_relay_invs offenseTracker is per connection (address incl. port, deleted on flow exit); offenses do not disconnect, so only a peer that voluntarily reconnects resets its count - ban leniency is maintainer policy
- checked safe: handle_relay_invs incoming reader (invChan drops when full, block/locator chans error when full), invsQueue capped at 5000 in readMsgBlock, orphan-root requeue bounded by orphan pool (600)
- checked safe: handle_relay_invs requestBlock bans on a block whose hash differs from the request, so unsolicited blocks queued in blockChan cannot be substituted
- user active on domain/utxoindex (08114be72, 8de5b30c6, 77b0e61fb on 2026-09-13): crash-level only; Update holds the index mutex, removes before adding, commits once
- note, not changed: htnwallet GetBalance holds RLock across GetBalancesByAddresses (bounded since HTN-151); pending is never populated so Pending is always 0 (functional gap, not a crash)
- checked: htnwallet keys Save lacks O_TRUNC/atomic rename/fsync (HTN-153 needs_human); trailing bytes harmless to load because ReadKeysFile uses json.Decoder.Decode; external_spendable_utxos/NewAddress/ShowAddresses fine
- checked: htnwallet daemon sign.go thin wrapper; holds s.lock (write) across DecryptMnemonics (slow KDF) so signing briefly blocks sync - minor, not changed
- checked: htnwallet autoCompound.go - broadcast failures back off and the next tick builds a fresh compound (no external double payment); rate-limited branch prints "backing off 30s" but returns without sleeping (minor, not changed). htnwallet CLI audit done
- checked: htnminer found-block handling (own header copy), timeout reconnect (guarded), 100ms template polling covers dropped notifications and lost subscription after reconnect; templatemanager.Get copies pow.State per hash - matrix/floatMatrix/cMatrix are fixed arrays and PoW methods never write them, so no race. Perf note, not changed: ~40KB (72KB arm64) copied under one global mutex per nonce attempt
- checked safe: grpcserver inbound count (decrement deferred right after increment); blocktemplatebuilder txselection (kaspad algorithm; contiguous float ranges, zero totalP progresses, fee/mass cubed cannot overflow)
- checked: wallet freezing is not exposed over RPC (only --freeze-address config flag); walletFreezingManager map access mutex-guarded
- checked: wallet freezing wiring (enabled by default, DAGParams set, default entry uses the hoosat: prefix EncodeAddress produces); --freeze-address replaces defaults and is unvalidated (HTN-159 needs_human)
- note, not changed: config.go whitelist parse is dead (guards on the parsed Config.Whitelists instead of Flags.Whitelists) but nothing reads Whitelists
- regression sweep 2026-09-14 23:40: all session-touched packages pass with exit status checked (29 ok)
- checked (autoupdate): archive extraction confined (HTN-161), atomic binary install (HTN-163), release verification missing (HTN-162 needs_human), os.Exit restart racing the DB lock (HTN-164 needs_human); CheckForUpdate/InstallUpdate have no external callers, overlapping downloads guarded by UpdateInProgress, Stop cancels ctx and waits. autoupdate audit done
- note, not changed: logger Backend.Close (deferred first in app.go, so runs last) closes writeChan while IsRunning stays true until the writer drains; a goroutine logging after Close panics ("send on closed channel" / "not running") - tiny window before process exit, DB already closed, kaspad same
- checked (HTN-165): LevelDB wrapper refuses pebble dirs; mirror case not real - pebble v2 refuses a LevelDB dir with "format major version 1 no longer supported" (not ErrCorruption), so NewPebbleDB does not wipe it; pinned by leveldb_directory_test.go (d8bc3f618). app removeDatabase only on --reset-db; checkDatabaseVersion records a version number, not the engine
- checked: cmd/utxoforensics has no datadir writes/deletes; tools/pruningproof-harness writes only its -out file; both open pebble read-write (WAL replay) - documented "run on a copy" in CLAUDE.md, ReadOnly would be a design change, not changed
- checked safe: domain/exodus diff.go + htnexodus diff --live - every UTXO iterator Get returns a fresh outpoint (DB iterators deserialize, collection iterator copies the pair, diff overlay forwards those), acceptance-data path passes outpointCopy; no aliasing in stored diffs
- FULL CI GATE GREEN 2026-09-15 02:10: gofmt repo-wide none, staticcheck subset ./... exit 0, go test -tags=ci -p 4 ./... exit 0 (101 ok, 0 failed)
- checked safe: compound_tx_rate_limiter addressTracker bounded - cleanupEmptyTrackers runs on every checkRateLimit / recordTransactionAt / isRateLimited
- checked safe: protowire RPC toAppMessage converters - nested derefs nil-guarded (x.Error), converters without nil receiver check return constant empty messages, AcceptedTransactionIds only used on repeated elements (never nil after unmarshal)
- checked safe: rpchandlers request-sized indexing/allocations (ban/unban IP[0] len-guarded, UTXO limits capped, capacity hints bounded by already-parsed request); message-size/exposure defaults filed as HTN-166 needs_human (P2P 4GiB deliberate in cff75920a, RPC 1GiB x 500 clients, RPC listens on all interfaces by default)
- checked safe: connmanager checkIncomingConnections trims only unrequested inbound (requested/outgoing removed from the set first); connectionSet keyed by IP:port (unique per TCP connection)
- checked: ban-true sites in flows are peer-provable except: wire-format heuristic and anticone query failures can ban on local errors (HTN-168 needs_human); ibd.go:30 bans only rule errors (fine); ibd.go:156 timeout ban and ibd.go:1283 low-rate ban are deliberate --enablebanning policy
- checked safe: appmessage RPCTransactionToDomainTransaction - protowire RpcTransaction/Input/Outpoint/Output/ScriptPublicKey converters all nil-guard, so PreviousOutpoint/ScriptPublicKey are never nil by the time it runs; callers are protowire-fed (submit, replacement, block) or node-built (verbosedata)
- checked safe: SubmitBlock converter path nil-guarded end to end (SubmitBlockRequestMessage -> RpcBlock -> RpcBlockHeader all check x == nil), so RPCBlockToDomainBlock never sees a nil header; hashes/parents/version parsed with errors checked
- checked safe: domain/prefixmanager DeleteInactivePrefix (deletes keys, then marker, then compacts; a crash leaves the marker so the next start resumes; cursor delete safe on both engines; markers live outside the prefix bucket)
- checked safe: infrastructure/os/limits (unix: early return when soft above want, errors if hard below min, Cur capped at Max with min fallback; windows/plan9 no-op)
- scan for [0] after upper-bound-only length checks (infrastructure, app, utxoindex, miningmanager, prefixmanager, exodus, util, cmd): only prefix.Deserialize (HTN-169) and app/db_version.go empty version file (HTN-170)
- audited safe: config file loading (override-dag-params devnet only; truncated default conf harmless), encodeHexString (always fresh buffer), DomainHash.UnsafeString aliases a pooled buffer but has no non-test callers (latent only), HandleIBDBlockRequests assertion (single-command route). HTN-115 parked needs_human (protocol acceptance rule); HTN-114 blocked on HTN-113 release
- audited safe: util bech32/DecodeAddress fuzzed 45s (no panics), NewAmount rejects NaN/Inf; bech32 does not reject non-zero padding bits (upstream behaviour, checksum covers raw data; note only)
- audited safe: rpchandlers get_usable_addresses / get_balance_by_address (full UTXOs scan + consensus filter per address, cached 30s / 1s; perf note only, deliberate), paginated/plain UTXO limit capped at 10M; rpcclient route/assertion scan done (HTN-175), appmessage Command() scan done (HTN-174), protowire converter return-type scan done (HTN-173)
- wire layer complete after HTN-174: 142 appmessage Command() types = 142 wire.go cases = 142 protobuf oneof payloads, each with toAppMessage and fromAppMessage
- audited safe: appmessage RPC->domain converters (protowire nil-guards output/input/UTXO entry before appmessage dereferences; hex/hash/subnetwork parse errors returned)
- audited safe: ldb FuseLevelDB fuse path (dest opened before Abs equality check, but opening writes nothing; a symlink spelling of a source fails on the LevelDB file lock, no data loss); htnexodus import keeps commitment-mismatch guard (rebase behaviour is needs_human territory, audited earlier)
- audited safe: grpcserver gzip_codec.go is only the stock grpc-go gzip compressor (blank import); compression_fallback map prunes expired entries on every insert (bounded)
- audited safe: grpcserver connection lifecycle (grpc-go v1.83.1 caps decompression at MaxRecvMsgSize; Disconnect CAS-once; closeSend closes ClientConn before the stream lock; both loops end via connectionLoops -> Disconnect). Note only: malformed HOOSAT_EXPERIMENTAL_DELAY env panics at first send (operator config) | re-checked 2026-09-15: experimental operator hook (connection_loops.go:53-58, same as kaspad), fails loudly at the first send; not changed
- latent risk note (no repro): router/route reuse (Router.Reset, Route.Reset, reusableIncomingRoutes) mixes traffic only if two live connections to the same outbound address overlap; routes are Close()d before recycling and forgetOutboundRouter drops the cache entry on disconnect (HTN-138). Handshake/ready/addressexchange assertions are on single-command routes
- audited safe: handshake error paths into protocol.go handleError (which panics on non-protocol errors): AddToPeers returns only ErrPeerWithSameIDExists; HandleReady returns only protocol/route-closed errors; ReceiveVersion/SendVersion non-protocol errors are local config only
- HTN-178 caller check: IsValid/IsRoutable are only called inside addressmanager (addressmanager.go, localaddressmanager.go with interface IPs of 4/16 bytes), so the length requirement rejects no legitimate address
- audited safe: dnsseed SeedFromDNS/SeedFromGRPC (seed IPs now pass the HTN-178 length check in AddAddresses; repeated protobuf message fields never decode to nil elements; mustPortUint16 only sees network params)
- audited safe: ConnectionManager.RemoveConnection panics "unimplemented" but has no callers (dead code)
- HTN-180 scan: fillInputs was the only peer-indexed Outputs[...] access in domain/miningmanager (blocktemplatebuilder indexes the local coinbase; mempool_utxo_set uses outpoints only as map keys)
- audited safe: transactionrelay HandleRequestTransactions (single-command route, IDs capped at MaxInvPerRequestTransactionsMsg by protowire), SharedRequestedTransactions (released by deferred RemoveMany and on enqueue failure, no leak), orphan pool promotion (fills from accepted parent outputs by own index), RBF conflict lookup. HTN-181 parked needs_human (unbounded invsQueue)
- audited safe: v8 HandleRequestBlockLocator, HandleRequestIBDChainBlockLocator (nil hashes -> full locator), HandleRequestAnticone, ReceivePings/SendPings all on single-command routes (assertions safe); a stray pong only bans the sender (nonce mismatch)
- HTN-182 parked needs_human: global isBusy held without timeout while waiting for the syncee in HandlePruningPointAndItsAnticoneRequests
- audited safe: consensus CreateBlockLocatorFromPruningPoint validates highHash exists and builds a logarithmic locator (peer limit only trims it); HandleIBDBlockLocator is on a single-command route
- HTN-183 parked needs_human: IBDBlockLocator hash count uncapped (honest locators logarithmic; no in-tree sender)
- audited safe: GetBlockTemplate RPC (template cache is commented out, so every call builds a fresh template - no shared in-place ModifyBlockTemplate mutation). Note only: RPC extraData length is not checked at build time, so an oversized value yields a template rejected at submit (operator-controlled input)
- note only (shutdown window): ComponentManager.Stop closes Domain().ConsensusEventsChannel() after netAdapter.Stop and protocolManager.Close; senders are consensus ValidateAndInsertBlock (consensus.go BlockAdded/virtualChangeSet) and rpc manager PruningPointUTXOSetOverride, and nothing waits for in-flight RPC handlers or flow goroutines past runFlows, so a block finishing during shutdown can panic on send to a closed channel (process is exiting; staged commits keep the DB consistent) | re-checked 2026-09-15: weaker than stated - netAdapter.Stop also stops the RPC server with GracefulStop (waits for in-flight handlers incl. submit_block and the pruning-point override sender) and protocolManager.Close waits for all flow routers (routersWaitGroup), so the known senders have finished before the close; not a clear bug, not changed
- audited safe: GitHub panic/error auto-reporting needs GitHubToken, which nothing configures (DefaultConfig empty, no flag) - CreateIssue/ReportError never send, so no config or secret leaks; HTND_PROFILER binds 127.0.0.1:6060
- user report 2026-09-15: TestInstallBinaryReplacesCurrentBinary failed on Windows (no exec bits) - fixed d6a7e2eee (skip exec-bit check on windows). GOOS=windows go vet ./... from linux: go-muhash and secp256k1/bip32 errors are cgo-disabled cross-compile artifacts, not Windows bugs; note only: util/memory/os_windows.go:16 vet "possible misuse of unsafe.Pointer" (VirtualAlloc result). Checking other session tests for POSIX-only assumptions (symlinks) before resuming HTN-184
- Windows portability pass over session tests: install_binary_test exec bits (d6a7e2eee), fresh_copy_test symlink (d19a26046); archive_escape_test portable (only checks nothing escaped); consensusstatemanager survey tests set HTND_UTXO_SURVEY=/dev/null only to activate in-memory records (no file opened), portable
- audited safe: util/memory/os_windows.go - go vet "possible misuse of unsafe.Pointer" is the expected warning for a VirtualAlloc address (non-Go memory the GC never scans or moves); sysFree releases via Block.mem, the original slice, so VirtualFree gets the reservation base address
- audited safe: cmd/htnctl (client-only; parseCommand checks parameter count, stringToValue returns errors for unsupported kinds, timeout bounded)
- audited safe: IBD syncee processBlockWithTrustedData already bounds-checks DAAWindowIndices / GHOSTDAGDataIndices against the received MsgTrustedData (banning protocol error; trusted_data_indices_test.go)
- peer-index grep (app/protocol, domain/miningmanager, infrastructure/network): no remaining unchecked peer-supplied slice indexes (IBD trusted-data indices checked, fillInputs fixed HTN-180, template builder indexes its own coinbase, ordered_transactions_by_fee_rate uses internal positions)
- audited safe: htnwallet daemon balance.go (index balances, pending always 0 by design), address.go (index bookkeeping saved before use), sign.go, external_spendable_utxos.go. Note only: broadcast of several transactions returns just the error when one fails midway, dropping the txIDs already submitted (a retry is rejected as duplicate) | fixed HTN-187 7c45a617c
- HTN-147-class scan: all 9 unchecked appmessage type assertions in app/protocol are on single-command routes (RequestTransactions, Addresses, IBDBlockLocator, RequestIBDChainBlockLocator, RequestAnticone, RequestBlockLocator, RequestRelayBlocks, RequestIBDBlocks, Reject) - safe
- 2026-09-15 user: "Continue from the critical issues" - taken as approval to fix the parked P2P DoS issues, in order HTN-182, HTN-181, HTN-183, choosing fixes that do not change which peers are disconnected or banned
- historical: remaining parked after critical pass: HTN-162 (release signature trust model - needs a key, not invented), HTN-166 (P2P 4 GiB message size deliberately raised by maintainer, IBD risk) stay needs_human; HTN-153 taken next (seed file non-atomic write, format unchanged)
- HTN-001 user direction 2026-09-15: pruning points depend on the CURRENT block version (chain-derived via blockversion.Current), not frozen at construction and not per evaluated block
- note only (latent, not on any real network): with a POWScores activation of 0, blockbuilder.blockVersionForDAAScore gives a DAA-score-0 block the raised version while blockvalidator.expectedBlockVersion special-cases DAA score 0 as version 1, so such a block fails ErrWrongBlockVersion; all real activation tables start above 0
- HTN-002 user decision 2026-09-15: measure first, no code (UTXOSetHealth via GetInfo); stays needs_human
- triage 2026-09-15 of HTN-003..012: HTN-008 (medium, dropped non-MissingTxOut error leaves block UTXO-valid) is a mechanical local-correctness fix - next after the HTN-001 commit; HTN-003 and HTN-010 need a version-semantics decision like HTN-001; HTN-004/005/006/007/009 need measurement or coordinated activation; HTN-011 deletion policy is a maintainer call; HTN-012 (bucket update atomicity) is a crash-safety fix in the pruning commit path, take after HTN-008. zsh note: unquoted $FILES does not word-split - use ${=FILES}
- HTN-003 user decision 2026-09-15: per-block rules (GHOSTDAG K, mass, DAA window, tip ordering, merkle sorting) use each block's own DAA-derived version
- historical: HTN-003 rule plan (2026-09-15): (1) GHOSTDAG - done, suite running; (2) block mass (header version) + accepted-ID merkle sorting (header version in validator, DAA-derived version of the block being built in builder) - tests+fix drafted in scratchpad/htn003m; (3) difficulty window/target time - selected parent computed DAA version (block own DAA score comes from the window), constructor gains powScores (only factory constructs it); (4) DAABlockWindow (consensus.go:1590, trusted data) - requested block version, overlaps HTN-010; (5) tip ordering resolve.go:70 and pick_virtual_parents maxBlockParents - virtual selected parent version, must match the header parents-limit rule; (6) non-consensus reads last
- HTN-009 user decision 2026-09-15: per-header version now for pruning-proof GHOSTDAG (same coordinated release; mixed-version IBD may fail until peers upgrade)
- version policy split (do not flatten): HTN-001 finality/pruning depths follow the chain's CURRENT version (blockversion.Current); HTN-003/HTN-010 rules and windows follow each block's own DAA-derived version; HTN-009 proof coloring follows each header's own version
- parked needs_human (do not act without asking): HTN-002, 004, 005, 006, 007, 011, 012, plus 114/115/116/146/153/159/162/164/166/168/177
- HTN-010 files: factory.go, dagtraversalmanager.go, window.go, pruningmanager.go, blockversion/current.go + daa_window_block_version_test.go, trusted_ghostdag_k_test.go; pruningproofmanager.go belongs to HTN-009
- historical: tree note (2026-09-15, truthful detail for next_action): the HTN-009 fix and proof_validation_version_test.go are already applied, uncommitted, in the working tree (factory.go, pruningproofmanager.go + the test); full -tags=ci domain+app suite exit 0, 71 ok (scratchpad/htn009_suite.log) - the apply step is done, remaining is commit (scratchpad/htn009/commit_msg.txt) -> committed 4e46244df
- note: user answers 2026-09-15 for HTN-004 (repro attempt), HTN-011 (wire in) and HTN-012 (fix) are recorded in ISSUES; their drafts exist only in scratchpad (work/, htn011/, htn012/), nothing in the tree - ask before applying. HTN-004 relay simulation seeds 100-139 (40 seeds) passed with no verdict disagreement; HTN-011 draft test hit ErrNegativeTarget from its shrunken DAA window (test setup, unresolved)
- user confirmed after HTN-009 commit: HTN-012 apply (tests guard the recovery path; divergence not reproducible on a healthy test DAG), HTN-011 wire in as authored (small depths never delete under mainnet depths - document it), HTN-004 build a reorg repro (test only)
- HTN-004 repro (2026-09-15): not reproduced - relay sim 40 seeds clean; restamp+cross-branch-spend reorg test (scratchpad/htn004) 20 attempts x 51 reconstructions, 3 arrival orders, all identical and matching commitments. Test committed f883de582 per user (issue kept open); coinbase-ID collisions not exercised.
- HTN-004 coinbase collision (2026-09-15): sibling coinbases below version 8 collide (confirmed); test with merge + spends on both branches passes 20 attempts. MUTATION FINDING: with isTolerableConflict returning false for every conflict, both HTN-004 tests (incl. committed f883de582) still pass - neither reaches resolveConflicts tolerance; f883de582 message overstates it as a guard for the tolerances. Tolerance needs diffs from different bases (offset/imported baselines, HTN-002/005), not reachable on a healthy test DAG.
- htnd5 node (2026-09-15 07:33): stopped by SIGINT per user for forensics (clean shutdown in log); was mining via RPC submit; binary /home/tonto/go/bin/HTND replaced since start - not restarted | 2026-09-15 ~07:39: the node was restarted outside this session (PID 500369, same args and appdir); forensics used the 07:34 copy taken before that | WARNING: that binary (/home/tonto/go/bin/HTND, installed 07:39:54) is vcs.revision 7c45a617c, unmodified - it contains every consensus commit of this session (ee22f57e9 HTN-001, e21594e04/12f307e87/e16ca9659/e99d965c7/04737e5cc HTN-003, 0765d2186 HTN-010, 4e46244df HTN-009, f56f96423 HTN-012, ba6d7f062 HTN-011) - a live mainnet node now runs rules meant for a coordinated release | user 2026-09-15: running these changes on htnd5 is ON PURPOSE; keep analyzing and fixing ISSUES.md
- 2026-09-15 ~08:45 user reports: (a) testing/integration FAIL (fix before next commit) - my pre-commit suites only ran ./domain/... ./app/...; from now run go test ./... (and -tags=ci) before commits; (b) ResolveVirtual extremely slow during clean sync (quick fix) - local IBD-style repro shows no regression vs 1425b20bb; need the slow node's log (version line, ResolveVirtual chunk/progress timings)
- integration finding: TestIBDWithPruning (non-ci only; SkipLongTest) hangs in mineNextBlockWithMockTimestamps -> mining.SolveBlock at ibd_test.go:260 on HEAD AND on pre-session base 1425b20bb - pre-existing, not this session's consensus changes. Cause: that helper always runs the Hoohash nonce search although the harness sets SkipProofOfWork (mineNextBlock already skips it). Fixing the helper in the tree.
- integration finding 2: in the full package TestIBDWithPruning failed again (ErrTimeTooMuchInTheFuture) though it passed alone - it indexed its overrides with constants.GetBlockVersion()-1, and earlier tests (TestIBD mining simnet blocks) raise the process-global to 2, so the overrides landed on index 1 while the pinned chain uses index 0. Fixed to index 0 / PruningDepthForBlockVersion(1). User 2026-09-15: "commit the fix" then "Wait for the test" - do not commit until the whole integration package (non-ci and ci) and go test ./... pass. TestUTXOIndex showed no log output for minutes in the running package - check whether it hangs.
- integration finding 3: TestUTXOIndex (non-ci) silent since 08:50 in the package run - it registers for miningAddress1 UTXOsChanged and does exactly 100 timeout-free receives for 100 mined blocks (own comment says 99 expected); NotificationManager.NotifyUTXOsChanged skips notifications with nothing for the listener's addresses (notificationmanager.go:214), so any virtual change without an addition for the address hangs it. Candidate: simnet version 2 after DAA 5 changing the coinbase payout. Waiting for the running package (user: wait for the test); then compare TestUTXOIndex on base 1425b20bb.
- HTN-003 leftover scan (2026-09-15): remaining global reads are fallbacks (no activation table / missing data) or non-consensus (cache sizes, hashrate log, nearly-synced threshold, mempool expiry, template sort/weighting, IBD mode decision, RPC checks); recorded in ISSUES; status left in_progress pending user
- historical: triage 2026-09-15 22:30: HTN-004 (diff-algebra tolerances are documented maintainer decisions; any change is consensus), HTN-005 (refusing non-matching buckets would stop all serving today), HTN-006/007 (measure first / coordinated activation), HTN-011 (--deletion-depth plumbed but CheckIfShouldDeletePastBlocks uncalled: wire or remove) and HTN-012 (confirmed crash-recovery re-pick against the live bucket; proposed: persist the chosen method before writing the bucket) all parked needs_human - questions for the user after HTN-010/HTN-009 are committed
- historical (applied to the HTN-101+ pass, not the current consensus pass): stay out of consensus/pruning/IBD (peer audit covers them); target mempool, tx relay, rpc, p2p non-consensus
- HTN-200 (2026-09-18, coinbase fee disagreement): user asked whether 1d380ebe8/175dfa4e6/e93758dde/4d31ea86d fix the "accepted block missing transaction fees" rejections. They do NOT. Validation always recomputes acceptance data (resolve_block_status.go:312 -> verifyUTXO), so TransactionAcceptanceData.Fee == totalIn-totalOut by construction (Fee is set from transaction.LoadFee() at calculate_past_utxo.go:322 right after ValidateTransactionInContextAndPopulateFee StoreFee's exactly that difference). acceptedFee()'s recompute therefore returns the same number as the old `txAcceptanceData.Fee`, and 4d31ea86d's incomplete-entries fallback is unreachable in both validation and (post-175dfa4e6) block building - currently dead code. git log ace480940..HEAD over coinbasemanager/blockbuilder/calculate_past_utxo/transaction_in_context shows no commit that changed the expected fee VALUE (4a1f26dce/91e64954d/71539ea40 are atomics only).
- HTN-200 evidence from the 2026-09-17 10:44 log: verifyUTXO order is utxo-commitment -> accepted-id-merkle-root -> coinbase (verify_and_build_utxo.go:97-110) and the verbose dump only prints when tolerate==false, so with the survey off both earlier checks PASSED: same past UTXO set, same accepted transaction set as the creator. Delta is fees only: +836000 miner, +44000 dev (5% of 880000) => merged-block fee total 880000 sompi this node counts and the creator counted 0. ACTUAL values are the exact pure subsidy split (1266666667/66666666 = 1333333333 with 5% dev fee), i.e. the creating node counted ZERO fees for EVERY merged block, not just one. Creator's coinbase payload decodes to 2.17.2-ace480940 (92 commits behind HEAD, 2026-09-13), so the log says nothing about whether 175dfa4e6 fixed OUR block creation - need a mismatch whose payload carries the new version.
- HTN-200 user input: "88,000 should be coming from 88 utxo outputs". Verified arithmetic: defaultMassPerSigOp=1000 (dagconfig/consensus_defaults.go:35) and mempool defaultMinimumRelayTransactionFee=1000 sompi/KB with minimumFee = mass*1000/1000 (check_transaction_standard.go:206), so a compound tx spending 88 UTXOs pays >= 88 sigops * 1000 = 88000 sompi from sigop mass alone. The observed 880000 is exactly 10x that - unresolved whether that is 10 compound txs in the merged block (defaultMaxCompoundTxPerAddressPerMinute=10, cmd/htnwallet auto-compound bursts) or this node counting one tx 10x / 10000 sompi per input too much. Do not guess further - the breakdown log below answers it.
- HTN-200 action taken: committed a8ddd9d17 "Log where the fees of a mismatching coinbase come from" - logMergeSetFeeBreakdown in verify_and_build_utxo.go prints, inside the existing verbose branch only, per accepted transaction: recorded Fee, fee implied by input UTXO entries, input count, entry count, entries-complete flag, plus complete per-merge-set-block totals (per-tx lines capped at 16/block). Diagnostic only, no value/rule/control-flow change. gofmt clean, go build ./... OK, coinbasemanager + blockbuilder + consensusstatemanager package tests pass.
- HTN-200 next_action: get one fresh "MERGE SET FEE BREAKDOWN" from htnd5 on a binary built at a8ddd9d17 or later. If recorded fee == implied fee and there are ~10 txs of 88 inputs each, 880000 is legitimate and the defect is entirely on the creating node (fee-less coinbase). If one tx shows recorded/implied 880000 with 88 inputs, this node over-counts by 10000/input and the defect is ours.
- HTN-200 concerns raised to the user about the four commits: (1) e93758dde runs RepairBlockStatuses on EVERY boot, setting every non-Invalid/non-HeaderOnly block to StatusUTXOValid (consensus.go:1856-1864) including StatusDisqualifiedFromChain and StatusUTXOPendingVerification, writing only the status and never a UTXO diff, with the error discarded at factory.go:653 - resolveBlockStatus short-circuits on UTXOValid, so those blocks never get a diff and the diff-child walk then hits the "blockHash is nil" family. (2) 4d31ea86d makes a consensus value depend on a recorded number rather than the UTXO set if it ever fires. (3) 1d380ebe8 gates the integer dev-fee split on the MERGING block's version in ExpectedCoinbaseTransactionInternal but on the BLUE block's version in coinbaseOutputForBlueBlockV2 - a v10 block merging v9 blues rounds differently in the two functions.
- HTN-200 breakdown result (2026-09-18 06:50:27, block f9c95d3b): ONE tx d5fdfa3d in merge set block e0573f77 - recorded fee 880000, implied by input entries 880000, 88 inputs, 88 entries, complete:true. The other two merge set blocks (5fbf56e9, 86612b18) carry 0. So this node's two fee numbers AGREE and the transaction genuinely pays 10000 sompi/input (~10x the 1 sompi/gram minimum, which for 88 sigops is ~88000). A second dump in the same second shows +19000/+1000 = 20000 = 2 inputs x 10000, same pattern. Our fee arithmetic is NOT wrong; the creating node credited zero.
- HTN-200 builder identified: the actual coinbase payload decodes to "2.17.2-320a23887/Mined via htn-stratum-bridge version pro_v1.7.0" - the commit immediately BEFORE 1d380ebe8/175dfa4e6/e93758dde/4d31ea86d, i.e. a node without 175dfa4e6.
- HTN-200 CONTRADICTION (unresolved, do not hand-wave past it): at 320a23887 the coinbase, the accepted-ID merkle root (newBlockAcceptedIDMerkleRoot) and the UTXO commitment (newBlockUTXOCommitment) ALL come from the same STORED virtual acceptance data / multiset - there is no fresh/stale mix, so 175dfa4e6's premise does not describe that code. Our validation passed both utxo-commitment and accepted-id-merkle-root before the coinbase check, so the builder's acceptance data had the same accepted set as ours, i.e. the compound tx WAS IsAccepted there, with Fee 0. Every writer of virtual acceptance data computes it fresh (update_virtual.go:95, validate_and_insert_block.go:78, blockbuilder 133 post-175dfa4e6) and Fee survives Clone, the LRU cache and the proto round trip (dbobjects.proto:80 fee=2; DomainTransaction.Clone copies LoadFee). So how Fee reached 0 there is still unexplained by code reading.
- HTN-200 experiment run: new test TestBuiltCoinbasePaysTheFeesOfTheMergeSet (committed 6e598567d) builds a block over a merge set carrying a fee and requires this node's own validator to accept it. PASSES at HEAD and PASSES at 320a23887 in a throwaway worktree. So the fee-less coinbase is state-dependent, not an unconditional builder fault, and 175dfa4e6 is not demonstrated to be its fix.
- HTN-200 next experiment (needs the user, cannot start the node myself): point the mining/template node at a HEAD binary and see whether its blocks are still rejected with the fee delta. If yes, creation is still broken at HEAD and the next step is a build-time check comparing the fee the coinbase credits with the fee total in the same virtualAcceptanceData. If no, 175dfa4e6 was the fix.
- HTN-200 ruled out: the coinbase is not a stale one from another template - checkCoinbaseBlueScore (block_body_in_isolation.go:93) enforces coinbase payload blueScore == header blueScore before UTXO verification, and it passed. Note though that validateCoinbaseTransaction deliberately copies the actual payload into the expected one (verify_and_build_utxo.go:548), so the payload's declared subsidy/extra data are not checked there; checkCoinbaseSubsidy (block_body_in_context.go:162) covers the subsidy, with a wide min/2..max*2 window for DAA scores in [43334184, 53334184).
- HTN-201 (new, low, separate): maybeAcceptTransaction's named return accumulatedMassAfter (calculate_past_utxo.go:356) is never assigned, so the accept path at line 464 returns 0 and applyMergeSetBlocks' accumulatedMass resets to zero after every accepted transaction. Nothing else reads it today, so the merge-set mass accumulation is inert rather than wrong - but it is not doing what it reads as doing.
- HTN-202 (2026-09-18, node does not answer htn-stratum-bridge, 192.168.1.170, latest code): ROOT CAUSE is e93758dde. RepairBlockStatuses runs inside factory.NewConsensus -> domain.New (component_manager.go:157), while the RPC manager is only built at component_manager.go:232, so the node is not listening for RPC until the walk finishes. The walk clears the status cache, reads every block's status from the DB and does a separate model.NewStagingArea + CommitAllChanges per changed block, holding s.lock. On an archival mainnet node that is effectively "dead node" from the bridge's point of view. Log signature: "Starting block status repair..." then "Processed N blocks, repaired M so far..." every 1000 blocks. FIXED ce46aa667: new hidden flag --repair-block-statuses (infrastructure/config/config.go, consensus.Config.RepairBlockStatuses, factory.go), default off, error no longer discarded. Recovery from disqualify-all-blocks mode now needs the flag passed explicitly once.
- HTN-200 new evidence 2026-09-18 04:12:59 (block 00ba4275, built by THIS node - payload decodes to "2.17.2-6e598567d-dirty/Mined via htn-stratum-bridge version v1.7.0", i.e. HEAD): failure mode is now "Output count differs: actual=2, expected=4", NOT a fee delta. Merge set = {0186ae63, bf0adbab}, both with 0 accepted transactions with inputs and 0 fees (breakdown log confirms). The built coinbase credits only 0186ae63 (miner script ace11d38); the validator demands 0186ae63 AND bf0adbab (miner script ed769550). So the builder dropped a whole merge set block. Note 0186ae63 was itself accepted via submit 1 second earlier with its coinbase check TOLERATED, and the node is processing ~1 block per 7 minutes.
- HTN-200 mechanism now identified as a MERGE SET / DAA-ADDED-BLOCKS asymmetry, not a fee-arithmetic fault: ExpectedCoinbaseTransactionInternal is called with blockHash=VirtualBlockHash at build time and blockHash=theBlock at validation time, so daaAddedBlocksSet, ghostdag merge set and acceptance data are all read for different blocks. calcMergedBlockReward returns 0 for any merge set block not in mergingBlockDAAAddedBlocksSet, and a 0 reward produces NO OUTPUT at all. This also explains the earlier fee cases without contradicting the passing utxo-commitment / accepted-ID-merkle-root checks: adding or moving a merge set block whose only transaction is a non-selected-parent coinbase changes neither the accepted transaction set nor the UTXO set, but does change which block the coinbase owes an output (or a fee) to.
- HTN-200 ruled out on the way: level 0 parents come from virtual's own parents (newBlockParents -> BuildParents with virtualBlockRelations.Parents), the block template cache in miningmanager.GetBlockTemplate is entirely commented out (every template is freshly built), and ModifyBlockTemplate is therefore unused - so no stale-template path.
- HTN-200 instrumentation committed 974904b06: ExpectedCoinbaseTransactionInternal now collects every merge set block that gets no output with the reason (no acceptance data / no reward + whether it is in the DAA added blocks set / no coinbase transaction / reward consumed by dev fee) and logs one Info line per coinbase build that drops any. Healthy builds log nothing. NEXT: get that line from a template build on 192.168.1.170 - if it says "no reward; in the DAA added blocks set: false" for the block the validator then demands, the fix is to build the coinbase for the block's own prospective merge set (what test_block_builder.go already does with a tempHash) instead of virtual's.
- HTN-200 FIXED bbcd32eb9 "Build a block's header and coinbase for that block, not for virtual". buildBlock now picks the parents first (still virtual's, via BuildParents), stages the prospective block under a temporary hash with its own relations + GHOSTDAG + DAA data, and derives the coinbase, accepted-ID merkle root, UTXO commitment, DAA score, blue score, blue work and required difficulty from THAT block. acceptanceData and multiset are passed into buildHeader instead of read back. Removed the now-unused newBlockDifficulty/newBlockAcceptedIDMerkleRoot/newBlockUTXOCommitment/newBlockBlueWork/newBlockBlueScore helpers. newBlockTime and newBlockCoinbaseTransaction now take a block hash.
- HTN-200 CRITICAL implementation note (cost me a regression, do not undo): the temporary hash MUST be derived from the parents, not a counter. Stores are read through per-store LRU caches keyed by block hash that are filled from the staging area on a plain READ - blockwindowheapslicestore.Get calls bss.cache.Add before any commit - so data computed for a temp hash outlives the discarded staging area. With a counter, TestCheckLockTimeVerifyConditionedByAbsoluteTime(+WithWrongLockTime) failed with ErrTimeTooOld: the template builder's counter collides with testBlockBuilder.nextTempBlockHash's counter over the same stores, so a cached window heap built for one parent set was served to a test block with different parents, shifting its past median time by 12s. prospectiveBlockHash() now hashes "prospective-block" + the parent hashes.
- HTN-200 verified equivalence: instrumented buildBlock printed virtual vs prospective for daaScore, blueScore, blueWork and selectedParent across a whole test chain - identical every time. So within one node the parents are always virtual's (BuildParents level 0 == the direct parents it is given) and the fix is a no-op in the normal case; its value is removing the class, including any staleness in virtual's stored GHOSTDAG/DAA data, which the node has been prone to during chunked resolve.
- HTN-200 test: TestBuiltCoinbasePaysEveryMergedBlock (blockbuilder) builds over a 2-sibling merge set and requires one reward + one dev fee output per merged block (only asserted from block version 2; version 1 buckets rewards) plus the node accepting its own block.
- GATE 2026-09-18: full `go test ./...` passes with AND without -tags=ci, plus ./testing/integration, ./cmd/..., ./tools/..., ./stability-tests/... (htnwallet needs -p 1 / 14G - the linker gets OOM-killed at -p 2 / 12G). This clears the gate owed for a8ddd9d17, 6e598567d, ce46aa667, 974904b06, bbcd32eb9 and the five older commits.
- 2026-09-18: ISSUES.md now carries HTN-200 (builder/validation disagreement, fixed bbcd32eb9), HTN-201 (store LRU caches filled on read serve a fake hash's data to another DAG - mitigated in the builder, general hazard open), HTN-202 (RepairBlockStatuses blocks RPC startup, fixed ce46aa667), HTN-203 (accumulatedMassAfter never assigned, open/low). 115 issues total. ISSUES.md and AGENT_STATE.md remain gitignored and unstaged.
- 2026-09-18: wrote AGENT_PROMPT.md at the repo root - the standing prompt the user pastes to start an autonomous session (wake protocol, working method, hard rules, operational constraints, stop conditions). It is UNTRACKED and not in .gitignore; leave it unstaged unless the user says otherwise.
- 2026-09-18: AGENT_PROMPT.md gained a "## Session input" section at the very end. Anything the user writes below its final "---" is for that session only and overrides next_action: start with HTN-NNN / skip ids / a new issue report (triage, next free HTN-NNN, record under `reported`, then work it) / answers to needs_human questions / a constraint for the day. A bare paste with no instruction is evidence. Step 2 of the wake protocol now says to read it first.
- HTN-204 probe technique: EstimateNetworkHashesPerSecond is a read-only way to measure the DAA window a live node can actually build - its answer stops changing once windowSize exceeds the real window length. Saturation point == virtualDaaScore - pruningPointDaaScore proved the window held only blocks above the pruning point
- HTN-204 arithmetic: difficulty 65536.01 IS mainnet genesisBits (0x1E7FFFFF); the powMax clamp shows 1.00. Any node reporting 65536.01 on mainnet/simnet is in the genesisBits fallback, not at a computed target
- htnctl refuses to talk to a node whose version differs; an untracked file makes the local build "-dirty". Use htnctl -a to bypass
- 2026-09-18 ~16:20 user: this agent has been moved to run ON 192.168.1.170 itself (hostname
  hoosat-network) rather than a separate dev box. `docker ps` here shows the real production stack:
  htnd-public (the mainnet node, "docker build running as mainnet node 1", up since ~13:20 this
  session), htn-stratum-bridge, htn-db-filler x2, htn-faucet, htnd-proxy, htn-rest-server,
  htn-explorer, htn-website, htn-discord-bot, htn-payment-gateway, htn-socket-server, htn-webwallet,
  pg16, htn-dns-seeder, htn-wiki, htn-vault - all "Up". `pgrep -a htnd` does NOT show htnd-public
  (it runs inside its own container namespace); check `docker ps` / `docker logs htnd-public` instead
  of pgrep to see whether the production node is busy. Never docker restart/stop it - same rule as
  the bare-metal node, just containerized now.
  User: "there is ram available to run more tests and nodes" - confirmed 188Gi available of 251Gi
  total (2026-09-18 16:20, htnd-public running). The old memory-pressure caution (4.3G available,
  Test16IncomingConnections OOM'd) no longer applies at this headroom - re-attempt long/skipped
  (non -tags=ci) integration tests here instead of treating them as permanently untestable, but
  still cap systemd-run MemoryMax explicitly and check `free -h` first since other things share
  this host now.
